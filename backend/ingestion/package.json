{
  "name": "@power-ranking/ingestion",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "daily": "tsx --env-file-if-exists=../../.env src/dailyUpdate.ts",
    "recompute": "tsx --env-file-if-exists=../../.env src/__tests__/manualRecompute.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@power-ranking/rating-engine": "*",
    "@power-ranking/shared": "*",
    "pg": "^8.13.0",
    "sharp": "^0.35.4"
  },
  "devDependencies": {
    "@types/pg": "^8.11.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
